# Core model + training
torch>=2.0
transformers>=4.30,<4.46  # 4.46+ 引入 CVE-2025-32434 检查，强制要求 torch>=2.6；我们用 torch 2.2 + safetensors 才能升
datasets>=2.14,<4.0
pytorch-crf==0.7.2
tqdm>=4.66,<5.0
rapidfuzz>=3.0,<4.0
psutil>=5.9,<8.0

# API service
fastapi>=0.100,<1.0
pydantic>=2.0,<3.0
uvicorn[standard]>=0.23,<1.0

# Test tooling
pytest>=8.0,<9.0
httpx>=0.27,<1.0

# Optional: ONNX export / quantization
# onnx>=1.14,<2.0
# onnxruntime>=1.15,<2.0
