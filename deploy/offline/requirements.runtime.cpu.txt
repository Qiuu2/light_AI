# Offline runtime requirements for the Kylin CPU-only bundle.
# Keep this file focused on service/runtime dependencies. Training and test
# dependencies stay in the repository root requirements.txt.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.8.0+cpu
transformers==4.57.3
datasets==4.4.2
pytorch-crf==0.7.2
tqdm>=4.66,<5.0
rapidfuzz>=3.0,<4.0
psutil>=5.9,<8.0

fastapi>=0.100,<1.0
pydantic>=2.0,<3.0
uvicorn[standard]>=0.23,<1.0
